const $=s=>document.querySelector(s);
async function init(){const id=new URLSearchParams(location.search).get("id");if(!id){fail("Falta el identificador del recurso.");return}try{const data=await VML.load();const r=(data.resources||[]).find(x=>x.id===id);if(!r){fail("No se encontró el recurso "+id);return}
if(r.id==="RE-001"){location.replace("laboratorio-matilde.html");return}if(r.id==="RE-047"){location.replace("laboratorio-blanca.html");return}if(r.id==="RE-058"){location.replace("laboratorio-emily.html");return}
const c=(data.creators||[]).find(x=>x.id===r.creatorId);$("#resource-kind").textContent=r.title||"Recurso educativo";$("#resource-title").textContent=r.creator||c?.name||"Recurso educativo";$("#resource-creator").textContent=`${r.id} · diseño educativo generado desde la Base Maestra`;$("#generic-status").textContent="Modo vista previa · recurso cargado desde Google Sheets";
const steps=String(r.sequence||"Explorar → contextualizar → interpretar → crear").split(/→|>|;/).map(s=>s.trim()).filter(Boolean);
$("#generic-content").innerHTML=`<div class="row g-4"><div class="col-lg-7"><div class="generic-box"><div class="eyebrow">Propósito</div><h2 class="h2 mt-2">Objetivo de aprendizaje</h2><p class="fs-5">${VML.safe(r.objective||"Objetivo pendiente de definición.")}</p><h3 class="h4 mt-4">Secuencia pedagógica</h3>${steps.map((s,i)=>`<div class="step"><span class="step-num">${i+1}</span><div><strong>${VML.safe(s)}</strong></div></div>`).join("")}<h3 class="h4 mt-4">Actividad propuesta</h3><p>${VML.safe(r.activity||"Actividad pendiente de diseño detallado.")}</p></div></div><div class="col-lg-5"><div class="generic-box mb-4"><div class="eyebrow">Contexto</div><p class="mt-3"><strong>Áreas:</strong> ${VML.safe(r.areas||"Por definir")}</p><p><strong>Nivel:</strong> ${VML.safe(r.level||"Por definir")}</p><p><strong>Obra base:</strong> ${VML.safe(r.workBase||"Por definir")}</p><p><strong>Tecnología:</strong> ${VML.safe(r.technology||"Por definir")}</p></div><div class="generic-box"><div class="eyebrow">Evaluación y accesibilidad</div><p class="mt-3"><strong>Evidencia:</strong> ${VML.safe(r.evidence||"Por definir")}</p><p><strong>Instrumento:</strong> ${VML.safe(r.assessment||"Por definir")}</p><p><strong>Accesibilidad:</strong> ${VML.safe(r.accessibility||"Por definir")}</p></div></div></div><div class="alert alert-warning mt-4"><strong>Diseño base.</strong> Este recurso todavía no equivale a una experiencia interactiva completa. Se genera automáticamente para que todas las creadoras puedan disponer de una propuesta educativa visible y revisable mientras se desarrollan experiencias enriquecidas.</div>${c?`<a href="archivo.html" class="btn btn-outline-brand rounded-pill">Volver al archivo de creadoras</a>`:""}`;}catch(e){fail(e.message)}}
function fail(m){$("#generic-status").textContent=m;$("#generic-content").innerHTML='<div class="alert alert-danger">No fue posible mostrar este recurso.</div>'}init();